<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.puppyroad.app.navi.mapper.NaviMapper">
	
	<select id="setCallDogNavi" statementType="CALLABLE" parameterType="NaviVO">
		{
			call navi_dogmap_prod(
				#{turnNo,		mode=IN,		jdbcType=DECIMAL},
				#{xdot,			mode=IN,		jdbcType=DOUBLE},
				#{ydot,			mode=IN,		jdbcType=DOUBLE},
				#{code,			mode=IN,		jdbcType=VARCHAR},
				#{c_navi,		mode=OUT,		jdbcType=CURSOR,		javaType=
			)
		}
	</select>
	
</mapper>