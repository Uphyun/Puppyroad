<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.puppyroad.app.petstarprofile.mapper.PetstarProfileMapper">
	<!-- 전체조회 -->
	<select id="selectProfileList" resultType="PetStarProfileVO">
		SELECT   profile_picture
				,gender
				,info
				,member_code
				,nickname
		FROM  pet_stargram_profile 		
		ORDER BY member_code DESC
	</select>
	<!-- 단건조회 -->
	<select id="selectPetStarProfileInfo" resultType="PetStarProfileVO">
		SELECT   p.profile_picture
				,p.gender
				,p.info
				,m.member_code
				,m.nickname
		FROM   pet_stargram_profile p JOIN  member m
									ON	p.member_code = m.member_code
		WHERE m.member_code = #{memberCode}
	</select>
	<!-- 등록 -->
	<insert id="insertProfile" parameterType="PetStarProfileVO">
		INSERT INTO pet_stargram_profile
			(
				 profile_picture
				,gender
				,info
				,member_code
				,nickname
		   )
		 VALUES
		   (
		   		#{profilePicture}
		   		,#{gender}
		   		,#{info}
		   		,#{memberCode}
		   		,#{nickname}
		   	)  
	</insert>
	
	<!-- 수정 -->
	<update id="updateProfile" parameterType="PetStarProfileVO">
		UPDATE pet_stargram_profile
		 SET
			profile_picture = #{profilePicture}
		   ,gender = #{gender}
		   ,info = #{info}
		   ,nickname = #{nickname}
		WHERE member_code = #{memberCode}
	</update>
	
	<!-- 삭제 -->
	<delete id="deleteProfile" parameterType="int">
		DELETE FROM pet_stargram_profile
		WHERE  member_code = #{memberCode}
	</delete>
</mapper>